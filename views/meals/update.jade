!!!
html
  head
    title= title
    script(type="text/javascript", src="https://ajax.googleapis.com/ajax/libs/jquery/1.6.2/jquery.min.js")
    script(type="text/javascript", src="/js/meals/update-client.js")
    link(rel="stylesheet", href="/css/meals/update-client.css")
  
  body
    form(action='/meals/update', method='post', id='meals-update')
      input(type='hidden', name='name', value=selfName)
      table
        thead
          tr
            th
            th Lunch
            th Dinner
        tbody
        each person in people
          tr(class= person.set? 'updated' : '')
            td= person.name
            td
              input(type='checkbox', name='lunch', checked=person.lunch, disabled=person.disabled)
            td
              input(type='checkbox', name='dinner', checked=person.dinner, disabled=person.disabled)
